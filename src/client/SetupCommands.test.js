const SetupCommands = require('./SetupCommands');
const sinon = require('sinon');

describe(`SetupCommands tests`, () => {

  describe(`getCommands function`, () => {

    it(`should return a Collection (Map) of commands`, () => {

      const deps = {
        Discord: { Collection: Map },
        fs: { readdirSync: () => }
      }
    });

  });

});